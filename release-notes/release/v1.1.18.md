## Correções

- Yomu: corrigida listagem após a API passar a retornar o catálogo codificado em base64 (`garimpo` / `catalogo`)
- Yomu: capítulos voltam a ser lidos do payload RSC (`capitulos_lista`)
- Yomu: resolução de ID de capítulo ajustada para a busca por slug na biblioteca

[versionCode:10046]
[channel:release]
